name: Auto Manager Feeder

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"  # 每 30 分钟运行一次，先稳定跑通

jobs:
  feed:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    concurrency:
      group: auto-manager-feeder
      cancel-in-progress: false

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run auto feeders
        env:
          DEEPSEEK_TOKEN_1: ${{ secrets.DEEPSEEK_TOKEN_1 }}
          DEEPSEEK_TOKEN_2: ${{ secrets.DEEPSEEK_TOKEN_2 }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          echo "Auto Manager Feeder started at $(date -u)"
          python auto_manager_feeder.py
          python auto_market_news.py
          echo "Auto Manager Feeder finished at $(date -u)"
